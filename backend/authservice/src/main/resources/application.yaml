server:
  port: 8081   

spring:
  application:
    name: authservice
    
  datasource:
    # 1. Connection settings
    url: jdbc:mysql://localhost:3306/ems
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver  
      # 2. Hibernate structural engine settings
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host : smtp.gmail.com
    port : 587
    username : akhilatavva15@gmail.com
    password : eqed mekh bqhx asdq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


      
            
        
secretKey: secretKeyForPasswordBCryotEncoding   
tokenExpiryTime : 60000

jwt:
   access-expiration: 900000
   refresh-expiration: 604800000
   
uiHostUrl: http://localhost:5173/   
        